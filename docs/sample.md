# Sample Document

This is a sample Markdown document for testing the RAG chatbot.

## Section 1

The chatbot uses retrieval-augmented generation to answer questions based on the content of Markdown files.

## Section 2

It chunks the documents, generates embeddings, and builds a vector index for efficient search.

## FAQ

Q: How does it work?
A: It loads Markdown files, splits them into chunks, embeds them, and uses a vector database to retrieve relevant chunks for answering queries.